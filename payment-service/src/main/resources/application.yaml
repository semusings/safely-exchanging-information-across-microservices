spring:
  cloud:
    schema-registry-client:
      cached: true
    stream:
      bindings:
        input: # your input sink channel name [SubscribableChannel]
          contentType: 'application/*+avro'
          destination: input
          group: order-payment-microservices
        createOrder: # your output sink channel name [MessageChannel]
          contentType: 'application/*+avro'
          group: order-payment-microservices
  datasource:
    url: jdbc:postgresql://localhost:5433/payments
    username: user
    password: password
server:
  port: 8081
